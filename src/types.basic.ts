const size: number = 1;
const isBig: boolean = size >= 100;
const msg: string = isBig ? "big" : "small";

const value: number[] = [1, 2, 3];

const v1: undefined = undefined;
const v2: null = null;
const v3: undefined | null = undefined;
